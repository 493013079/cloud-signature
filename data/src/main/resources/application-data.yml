spring:
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: none
  data:
    redis:
      repositories:
        enabled: false
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

