spring:
  jpa:
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
        format_sql: true
    hibernate:
      ddl-auto: none
  data:
    redis:
      repositories:
        enabled: false
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE
